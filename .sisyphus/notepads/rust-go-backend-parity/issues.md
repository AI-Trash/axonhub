2026-03-27: Rust release/docs posture remains an explicit parity gap even where runtime coverage exists; README/README.zh-CN, AGENTS, backend rules, and Helm docs still frame Rust as a migration slice and Go as required fallback/truth source.
2026-03-27: Route-level parity must be measured separately from behavior parity; GraphQL breadth, Ent-backed persistence surface, provider-edge capability availability, GC, provider-quota polling, and legacy-only dialects remain uncovered even when Rust exposes matching entrypoints.
2026-03-27: The local task environment did not have `go` on PATH for Python subprocesses at first, so Task 2 verification was blocked until Go 1.26.1 was installed via Homebrew and the runner could execute real Go oracles.

- `lsp_diagnostics` for the touched Go harness file could not run because `gopls` is not installed in this environment; direct `go test ./.sisyphus/parity -run ^TestParityOracleEmitSuite$ -count=1` was used instead as the compile/behavior check.
- Invoking `rustfmt` directly on a file without `--edition 2021` used an older default edition in this environment and failed on `async fn`; rerunning with `rustfmt --edition 2021 apps/axonhub-server/src/app/parity_oracle.rs` fixed formatting.
- The current oracle emitters are still limited to the three existing HTTP-backed emitters (`admin_system_status_initial`, `v1_models_basic`, `openapi_graphql_create_llm_api_key`); later tasks can add more fixture-driven emitters without changing the Python harness shape.
- 2026-03-27: Introducing a new direct Rust dependency for task-scoped CLI work caused `cargo test` to rewrite the workspace `Cargo.lock`; to stay within the task’s allowed file list, the lockfile had to be restored after verification.
- 2026-03-27: Task 4 still has a truthful broader parity caveat: the existing oracle suite `config_get_cache_default_expiration_alias` continues to report `mismatched as expected`, so the evidence file records that named suite as a current cross-runtime mismatch rather than claiming full Go parity there.
- 2026-03-27: Capturing Task 4 evidence with `cargo run -p axonhub-server --bin axonhub -- ...` produced heavy unrelated Rust warnings in this tree, so the final artifacts use `target/debug/axonhub` command output for cleaner deterministic evidence while keeping behavior identical.
- 2026-03-27: `.toml` files in this workspace do not have a configured LSP server, so `lsp_diagnostics` could not cover `crates/axonhub-db-migration/Cargo.toml`; the test-only dependency change there was validated by successful cargo test runs instead.
- 2026-03-27: `cargo test -p axonhub-server foundation -- --nocapture` still emits pre-existing unused-import/dead-code warnings in unrelated foundation files (`foundation/repositories/identity.rs`, `foundation/repositories/openai_v1.rs`, `foundation/identity.rs`, `foundation/seaorm.rs`, etc.); the task-specific Rust sources stayed at zero LSP diagnostics.
- 2026-03-27: MySQL runtime wiring is now recorded as supported rather than future-only, but this non-Docker session did not execute a live MySQL connection or migration run; the evidence file calls that limitation out explicitly instead of overstating verification.
2026-03-30: The Wave 1 `parity_inventory.py validate` output is still a historical gap report, not a truthful Task 16 pass/fail gate; even after fixing its deleted `handlers/unported.rs` assumption, it continues to encode earlier partial/gap states for broader repo posture, so the final Task 16 enforcement moved to a dedicated consolidated regression driver instead of treating the inventory report as the final verdict.
2026-03-30: `lsp_diagnostics` could not validate `.sisyphus/parity/suites.json` because the configured JSON LSP (`biome`) is not installed in this environment; JSON correctness was instead validated by successful parity-driver runs that parsed the manifest and fixtures end-to-end.
2026-03-30: The Task 1 inventory artifacts cannot serve as a no-gap proof for Task 16 while they still encode wider repo posture mismatches like `/favicon`, provider-edge partials, and release/docs gaps. The final fix kept them as contextual inventory evidence only and removed the no-gap claim from both the generator truth model and the Task 16 regression decision logic.
2026-03-30: Task 18 evidence-index generation is order-sensitive: if it runs before `.sisyphus/evidence/task-18-guardrails.txt` exists, it truthfully reports that artifact as missing. The durable fix is to execute the guardrail command before the evidence-index command in CI and local verification.
2026-03-30: Markdown LSP is still unavailable for the maintained English docs, so this Task 18 repair had to validate doc truth through grep plus the guardrail/evidence commands rather than editor diagnostics.
2026-03-30: Even after widening Task 18 to maintained English docs, a few narrower contradiction strings still slipped through because the guardrail patterns were too general. Exact phrase checks were needed for the backend-rules line, the `internal/server/` wording, the channel-addition guidance, and the `/v1/images/edits` API docs.
