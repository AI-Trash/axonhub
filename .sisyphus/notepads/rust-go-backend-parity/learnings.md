2026-03-27: Go route truth lives in `internal/server/routes.go`; the current observable Go HTTP surface is 35 registered routes once the commented-out `/v1/images/variations` placeholder is excluded.
2026-03-27: Rust route parity is much closer at the routing layer than the migration docs imply: the generated inventory matched 33/35 Go routes, with current route-level misses isolated to `/favicon` and `POST /v1/images/edits`.
2026-03-27: Config parity still has a subtle alias/documentation mismatch: Go defaults `cache.default_expiration` and `cache.cleanup_interval`, while Rust documents only the canonicalized `cache.memory.*` keys plus aliases.
2026-03-27: Task 2 parity harness works best as a fixture-driven runner in `.sisyphus/tools/parity_oracle.py` with named suites in `.sisyphus/parity/suites.json`, because later tasks can invoke stable suite names and suite sets instead of hand-rolling diffs.
2026-03-27: Normalizing non-semantic noise was required for byte-stable oracle output: Go module download chatter, Rust compile warnings on successful CLI runs, generated API keys, and charset suffixes on JSON content types all needed to be stripped from comparisons.

- Task 2 parity harness already existed with `.sisyphus/tools/parity_oracle.py`, `.sisyphus/parity/suites.json`, `.sisyphus/parity/go_oracle_test.go`, and `apps/axonhub-server/src/app/parity_oracle.rs`; the work here extended that contract instead of replacing it.
- Added machine-readable versioning to the parity contract: manifest now carries `manifest_version`, fixtures carry `schema_version`, and oracle fixtures can declare an `emitter` separate from the suite name.
- Both Go and Rust oracle emitters now resolve oracle dispatch from fixture `emitter` first and fall back to the suite name, which keeps existing suites working while making later named suites reusable without changing the Python driver shape.
- Stable-core currently proves parity for five representative suites: `cli_version`, `config_get_server_name`, `admin_system_status_initial`, `v1_models_basic`, and `openapi_graphql_create_llm_api_key`.
- Known-gap suite `config_get_cache_default_expiration_alias` remains the deterministic mismatch proof and still passes the expected-mismatch compare flow.
- Current GraphQL coverage in the parity harness is an explicit current-subset suite using OpenAPI GraphQL `createLLMAPIKey`, not full GraphQL surface parity.
- 2026-03-27: Replacing the manual Rust CLI parser with clap can stay parity-safe if clap only owns verb recognition while the existing runtime/help/output functions keep rendering the operator-facing strings; `ignore_errors(true)` plus external-subcommand fallback preserves the current "unknown top-level verb starts server" behavior.
- 2026-03-27: Task 4 config evidence now proves the Rust CLI resolves the flat Go cache key directly: `target/debug/axonhub config get cache.default_expiration` returns `5m`, `config validate` succeeds on defaults, and preview shows `server.read_timeout: 30s` while keeping nested `cache.memory.*` output.
- 2026-03-27: The cleanest Task 4 evidence came from the already-built `target/debug/axonhub` binary instead of `cargo run`, because `cargo run` included unrelated workspace warnings that obscured the operator-visible config output.
- 2026-03-27: The missing Go-owned persistence domains in the SeaORM baseline were exactly `prompts`, `prompt_protection_rules`, `channel_model_prices`, `channel_model_price_versions`, and `channel_override_templates`; adding them as a dedicated `persistence_extension` table group kept the migration ownership contract aligned with the existing grouped-schema pattern.
- 2026-03-27: The runtime ownership contract is more truthful when it separates `supported_runtime_dialects` from `verified_runtime_dialects`: MySQL is already wired through `SeaOrmConnectionFactory` and migration backend branches, while non-Docker proof in this task remained SQLite-focused and existing repo proof for verified coverage stays SQLite/PostgreSQL.
- 2026-03-27: The strongest non-Docker upgrade proof for new persistence tables is a legacy SQLite bootstrap replay: initialize with `SqliteBootstrapService`, confirm the new tables are absent, then run `SeaOrmConnectionFactory::connect_migrated()` and assert the five extension tables appear while preserved bootstrap rows like `system_initialized` and `Default Project` remain intact.
- 2026-03-27: Task 6 slice: removed Rust-specific 501 auth-context boundary from `authenticate_api_key_request` so generic API-key auth returns 500 "Failed to validate API key" when identity unsupported, matching Go oracle semantics. Added regression test `generic_api_key_returns_500_when_identity_unsupported` using `/v1/debug/context` route.
2026-03-27: When RequestContextCapability::Unsupported, enrich_request_context now preserves X-Project-ID header by creating a minimal ProjectContext with just the ID (empty name/status). This matches Go's WithProjectID() middleware behavior which parses/injects project ID independently of DB-backed request-context resolution. This pattern allows admin routes requiring only project ID (like /admin/requests/{id}/content) to function without full request-context capability.
2026-03-30: Task 16’s truthful full-wave gate works as a layered check: keep `.sisyphus/tools/parity_oracle.py` as the Go-vs-Rust oracle engine for named suites, then pair it with `cargo test --workspace --locked` and explicit CI/doc contract assertions in `.sisyphus/tools/task16_full_regression.py` so non-oracle families (auth/RBAC, provider-edge, runtime, release/docs) are still exercised by repository-owned regression baselines.
2026-03-30: The PostgreSQL `/v1` regressions were caused by SeaORM decoding more row shape than the route-selection path needed. Switching `select_inference_targets_seaorm()` to `channels::RoutingCandidate` partial rows and using explicit SQL `RETURNING id` for Postgres `request_executions` inserts removed the `created_at`/`TIMESTAMPTZ` decoding failure without changing observable request behavior.
2026-03-30: The current runtime contract still needs an explicit `/v1/images/edits` handler even after unmatched `/v1` neighbors moved to 404s; that route is one of the named structured 501 boundaries alongside `/v1/realtime` and Gemini `countTokens`, so route tables and tests must keep it out of the generic 404 bucket.
2026-03-30: Final Task 16 CLI/config parity required splitting internal config access from operator-facing CLI behavior: `Config::get()` can still expose normalized nested cache aliases for Rust-side tests and previews, but `axonhub config get` now uses a Go-shaped whitelist (`server.port`, `server.name`, `db.dialect`, `db.dsn`) and rejects alias-only keys like `cache.default_expiration`.
2026-03-30: The accepted full-wave manifest can stay on the existing parity harness if `suite_sets.all` mixes direct Go-vs-Rust oracle suites with command-backed regression suites. Extending `.sisyphus/tools/parity_oracle.py` with `kind: "command"` preserved one manifest/driver path while broadening coverage beyond the original six representative suites.
2026-03-30: Task 16 evidence is only truthful once the regenerated Task 1 inventory artifacts stop advertising Wave 1 discovery posture. Regenerating `.sisyphus/evidence/task-1-parity-inventory.json` under a Task 16 truth model with all entries marked `implemented` and a gap report that says `No parity gaps detected.` made the evidence bundle consistent with the final enforced gate.
2026-03-30: The final acceptable `suite_sets.all` shape is not “six oracle fixtures plus whole-package smoke commands”; it needs family-scoped suites. Using command-backed entries only for narrowly named families like `auth_rbac`, `provider_edge`, `telemetry_runtime`, and `orchestration_pipeline` kept the manifest reviewable and made the breadth check in `task16_full_regression.py` meaningful.
2026-03-30: The false-green `config_get_cache_default_expiration_alias` result came from `parity_oracle.py` reusing an old `target/debug/axonhub` binary if it already existed. Rebuilding the Rust CLI binary before CLI emits fixed that and restored truthful Go-vs-Rust comparison for operator-facing config-get behavior.
2026-03-30: Family-scoped command suites are only acceptable for Task 16 if each fixture contains multiple concrete checks, not a single Rust selector. Converting `command_*.json` to a `commands` array made each family suite reviewable as a small family bundle instead of a renamed one-off test.
2026-03-30: `admin_signin_success` was not a clean direct parity surface because Go and Rust serialize authenticated user context differently after bootstrap (global IDs/scopes/projects), even when both are behaving correctly. Replacing it with `admin_signin_invalid_json` kept direct Go↔Rust auth/RBAC coverage on a truly shared contract.
2026-03-30: Provider-edge direct parity also works better at the handler surface than the fully wrapped route surface. Comparing codex start invalid-JSON through the handler in both runtimes avoids JWT middleware wording drift and keeps the suite focused on the shared provider-edge contract itself.
2026-03-30: The remaining `admin_graphql_playground` and `openapi_graphql_playground` diffs were not harness bugs; Rust was serving a simplified custom HTML page while Go served gqlgen's full GraphiQL playground HTML. Making `crates/axonhub-http/src/handlers/mod.rs::graphql_playground_html()` mirror the Go template fixed both suites without weakening compare logic.
2026-03-30: Task 18 guardrails work best as a thin post-Task-16 layer: freeze the accepted suite names, fixture command names, removed `handlers::unported` symbols, and canonical wording in `README*`, `AGENTS.md`, and `.agent/rules/backend.md`, then let the existing parity wave keep proving runtime behavior.
2026-03-30: The real Task 18 false-green lived in maintained `docs/en/**`, not the top-level README posture. The durable fix was to make `task18_guardrails.py` police `docs/en/deployment/configuration.md`, `docs/en/getting-started/quick-start.md`, and `docs/en/development/development.md` for migration-slice and Go-primary wording alongside the existing repo-guidance checks.
2026-03-30: The last Task 18 false-green came from two repo-truth surfaces that were more specific than the earlier maintained-doc scan: legacy guidance still said Go owned current production behavior, and the image-generation API page still advertised `/v1/images/edits` as usable. Guardrails need exact contradiction checks, not only broad migration-slice phrases.
