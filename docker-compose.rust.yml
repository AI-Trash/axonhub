services:
  axonhub:
    image: looplj/axonhub:rust-latest
    container_name: axonhub-rust-app
    ports:
      - "8090:8090"
    volumes:
      - axonhub-rust-data:/app
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--no-verbose",
          "--tries=1",
          "--spider",
          "http://localhost:8090/health",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

volumes:
  axonhub-rust-data:
